version: "3"
services:
  webapp:
    container_name: "webapp"
    ports:
      - "3000:3000"
    build: ./
    links:
      - mongo
    depends_on:
      - mongo
  mongo:
    image: mongo
    volumes:
          - /data/mongodb/db:/data/db
    ports:
      - "27017:27017"