version: "3"
services:
  web:
    build: ./
    ports:
      - "3000:3000"
    links:
      - mongodb
    depends_on:
      - mongodb
  db:
    image: mongo:latest
    volumes:
      - /data/mongodb/db:/data/db
    ports:
      - "27017:27017"