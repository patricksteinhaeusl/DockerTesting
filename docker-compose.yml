version: "3"
services:
  webapp:
    build: ./
    ports:
      - "3000:3000"
  database:
    image: mongo:latest
    volumes:
      - /data/mongodb/db:/data/db
    ports:
      - "27017:27017"
    depends_on:
      - webapp